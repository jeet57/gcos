/**
 * @gcos/ui — shared design system components
 *
 * M01 scope: package exists and builds cleanly so apps/web can depend
 * on it via workspace:*. shadcn/ui primitives (Button, Card, Badge,
 * Input, Dialog, Table) and pattern components (ReadinessScoreBadge,
 * StreakCounter, etc.) are added in M05 and M06.
 */
export const GCOS_UI_PACKAGE_VERSION = '0.0.1';
